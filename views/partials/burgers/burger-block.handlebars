<li class="list-group-item">
	{{burger_name}}

	<button class="devour-state btn" data-id="{{id}}" data-devoured="{{devoured}}">
		{{#if devoured}}DEVOUR{{else}}EAT AGAIN!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>
